module.exports = (data) => ({
  id: `${data.namespace}/${data.name}/${data.provider}/${data.version}`,
  owner: data.owner,
  namespace: data.namespace,
  name: data.name,
  version: data.version,
  provider: data.provider,
  description: '',
  source: data.location,
  published_at: data.published_at,
  downloads: 0,
  verified: false,
  root: {},
  submodules: [],
});
